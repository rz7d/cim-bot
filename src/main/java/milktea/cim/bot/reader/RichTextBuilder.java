package milktea.cim.bot.reader;

public class RichTextBuilder {

  public RichTextBuilder() {
  }

  // var group = matcher.group();
  // int count = group.length();
  // event.getChannel().sendMessage(
  // new EmbedBuilder()
  // .setTitle("hmage発見器")
  // .setDescription(
  // "\"" + group + "\" を発見しました: "
  // + "```"
  // + msg + " \n"
  // + repeat(" ", matcher.start()) + repeat("-", count)
  // + repeat(" ", msg.length() - (matcher.start() + count))
  // + " " + "(" + count + ")"
  // + "```")
  // .setFooter(event.getMessage().getAuthor().getName(),
  // event.getMessage().getAuthor().getAvatarUrl())
  // .build())
  // .complete();

}
